# Full repo comparison: this fork vs [ZealanL/GigaLearnCPP-Leak](https://github.com/ZealanL/GigaLearnCPP-Leak)

Generated by diffing a shallow clone at `c:\GigaLearn\_upstream_GigaLearnCPP-Leak` against `GigaLearnCPP-Leak-Ref`.  
**Ignored:** `build/`, `.git/`, checkpoints, teacher weights.

---

## 1. High-level summary

| Area | Same as upstream? | Notes |
|------|-------------------|--------|
| **RLBotCPP/** | **Yes** (no diff in tree compare) | Headers, flatbuffers, CMake match |
| **GigaLearnCPP/** (core lib) | **Mostly** | **19 files** differ — see §3 |
| **Root `src/`** | **No** | Only `ExampleMain.cpp` — **large** fork-specific entry |
| **Root `CMakeLists.txt`** | **No** | CUDA `nvToolsExt` shim for `find_package(Torch)` |
| **Docs** | N/A | `docs/` exists **only in this fork** |

---

## 2. Root & entrypoint

### `CMakeLists.txt` (project root)

| Upstream | Fork |
|----------|------|
| Minimal exe + subdirs | Adds **CUDA::nvToolsExt** workaround block so LibTorch CMake can resolve on Linux / CUDA 12.x |

### `src/ExampleMain.cpp`

| | Upstream | Fork |
|---|----------|------|
| Size | Minimal demo (~200 lines) | ~370 lines |
| Rewards / obs | Simple `AdvancedObs`, small reward list | **CustomObs**, large reward stack, `CombinedState` |
| Arena | Fixed 1v1 | **`--players-per-team`** 1–3 |
| Device | GPU in sample | **`--cpu` / `--gpu`**, default CPU |
| PPO | Small batch demo numbers | Production-scale `tsPerItr`, nets, checkpoints, skill tracker |
| TL | None | **`--tl`**, `StartTransferLearn`, teacher path resolution |
| RocketSim | Hardcoded mesh path | **`FindCollisionMeshesPath()`** |

---

## 3. `GigaLearnCPP/` — every differing file (19 total)

Command used:

`git diff --no-index --stat _upstream/.../GigaLearnCPP GigaLearnCPP-Leak-Ref/GigaLearnCPP`

### Build / tooling

| File | Fork vs upstream |
|------|-------------------|
| **`GigaLearnCPP/CMakeLists.txt`** | Filters **`TORCH_LIBRARIES`** entries matching `NOTFOUND` (Linux link fix). **`python_scripts`**: `file(COPY ...)` + MSVC POST_BUILD copies next to DLL. Optional **CUPTI DLL** copy for MSVC. |
| **`RLGymCPP/CMakeLists.txt`** | Tiny diff (line endings / minor). |
| **`RocketSim/CMakeLists.txt`** | Tiny diff. |

### RocketSim / RLGym (sim + includes)

| File | Fork vs upstream |
|------|-------------------|
| **`RocketSim/.../BoostPad.h`** | `prevLockedCarID = NULL` → **`0`** (valid `uint32_t`; fixes warnings/UB). |
| **`RLGymCPP/.../CommonValues.h`** | `#include "../Framework.h"` → **`"Framework.h"`** (include layout). |
| **`RLGymCPP/.../EnvSet.h`** | **`OBSBuilders` → `ObsBuilders`** path casing (Linux-friendly). |

### RLGym — **new / huge** (fork-only content)

| File | Notes |
|------|--------|
| **`ObsBuilders/CustomObs.h`** | **Not present upstream** — full custom observation (student / TL alignment). |
| **`ObsBuilders/AdvancedObsPadded.h`** | **Not present upstream** — padded obs variant. |
| **`Rewards/CommonRewards.h`** | **Massive** fork-specific reward implementations (thousands of lines vs upstream’s smaller set). |

### PPO / learning core

| File | Fork vs upstream |
|------|------------------|
| **`PPO/ExperienceBuffer.h`** | Extra overload **`_GetSamples(torch::Tensor indices)`**. |
| **`PPO/ExperienceBuffer.cpp`** | **CUDA-correct** indexing: indices on same device as tensors; shuffle builds **one** `tIndices` on device; slices per batch (performance + correctness). |
| **`PPO/GAE.cpp`** | `returnStd` guards; **finite** sanitization; **no read past end** of `valPreds` at last step (upstream UB). |
| **`PPO/PPOLearner.h`** | `#include "ExperienceBuffer.h";` → **`ExperienceBuffer.h`** (drop stray `;`). |
| **`PPO/PPOLearner.cpp`** | **`Learn()`** matches upstream structure (`InferPolicyProbsFromModels` + `InferCritic`) + **`curBatchSize`** for ratios/minibatches. Defensive **prob** sanitization in `InferPolicyProbsFromModels` / `InferActionsFromModels`; safer **`ComputeEntropy`**. |

### Stats / models / time

| File | Fork vs upstream |
|------|------------------|
| **`Util/WelfordStat.h`** | Skip non-finite samples; safe **GetMean/GetSTD**; `BatchedWelfordStat` row guard. |
| **`Util/Models.h`** | **`ModelSet::ZeroGrad()`**, **`Model::ZeroGrad()`**; `ModelIterator` modernized (no deprecated `std::iterator`). |
| **`Util/Models.cpp`** | **`Model::ZeroGrad()`** implementation. |
| **`Util/Timer.h`** | **`steady_clock`** instead of `high_resolution_clock` for elapsed time (monotonic behavior on Linux). |

### Learner (rollout + metrics)

| File | Fork vs upstream |
|------|------------------|
| **`Learner.cpp`** | Python **sys.path** (cwd + exe dir); **LoadStats** optional file; rollout **progress logs**; **obs norm** finite guards; **`StepFirstHalf(false)`** + removed redundant **Sync** (freeze fix); **`lastRewards[arenaIdx]`** for reward metrics; GAE/consumption path guards and tensor handling differences. |

---

## 4. What matches upstream exactly (within `GigaLearnCPP/`)

Everything **not** listed in §3 is **byte-for-byte same path** in the diff stat (hundreds of files: most of GigaLearnCPP public API, PolicyVersionManager, MetricSender, much of RLGymCPP, RocketSim sim code, etc.).

---

## 5. How to reproduce the full diff

```powershell
cd C:\GigaLearn
git clone --depth 1 https://github.com/ZealanL/GigaLearnCPP-Leak.git _upstream_GigaLearnCPP-Leak

git diff --no-index --stat `
  _upstream_GigaLearnCPP-Leak\GigaLearnCPP `
  GigaLearnCPP-Leak-Ref\GigaLearnCPP

git diff --no-index --stat `
  _upstream_GigaLearnCPP-Leak\src `
  GigaLearnCPP-Leak-Ref\src

git diff --no-index `
  _upstream_GigaLearnCPP-Leak\CMakeLists.txt `
  GigaLearnCPP-Leak-Ref\CMakeLists.txt
```

---

## 6. Related docs in this fork

- `FRAMEWORK_VS_ZEALAN_UPSTREAM.md` — PPO/GAE/Learner narrative
- `LEARNING_VS_PPOLearnerConfig.md` — `ExampleMain` vs `PPOLearnerConfig` defaults
